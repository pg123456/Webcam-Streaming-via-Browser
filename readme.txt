For the host-side client, ignore all text shown on the HTML page. 

The code provided from https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API/Taking_still_photos was used as the starting point for the host-side client and was heavily modified to meet the requirements of the project.

The project itself was intended for educational purposes only. 